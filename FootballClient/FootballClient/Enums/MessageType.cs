namespace FootballClient.Enums
{
    public enum MessageType
    {
        CREATE_INVITE,
        ACCEPT_INVITE,
        DECLINE_INVITE,
        REJECT_INVITE,
        SUCCESS_INVITE,
        FAIL_INVITE,
        RECIEVED_INVITE,
        ERROR
    }
}
